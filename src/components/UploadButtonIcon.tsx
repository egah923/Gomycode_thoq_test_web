import UploadCamera from './Icons/UploadCamera'

const UploadButtonIcon = () => {
  return (
    <button style={{ border: 0, background: 'none' }} type="button">
      <UploadCamera />
    </button>
  )
}

export default UploadButtonIcon